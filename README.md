
  # Start app development (Copy)

  This is a code bundle for Start app development (Copy). The original project is available at https://www.figma.com/design/jm1nERse2k1sA0ngrqjsAa/Start-app-development--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  